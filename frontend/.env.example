# Optional. Leave empty for same-origin /api (local Vite proxy or Railway Docker).
# On Vercel, set this to the public FastAPI origin (no trailing slash), e.g.:
# VITE_API_BASE_URL=https://your-service.up.railway.app
VITE_API_BASE_URL=
